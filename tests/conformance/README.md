# Conformance Suite

Runtime conformance tests are implemented in `services/runner/tests/conformance` and validated via `go test ./...` in `services/runner`.

This directory anchors the repository-level conformance test namespace required by the execution spec.
