# Federation Topology Map CLI topology export:
- `reachctl federation map --format=json`
- `reachctl federation map --format=svg`

Includes node IDs, trust, compatibility, quarantine state, and delegation health snapshots.
