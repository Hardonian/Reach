# Proof Mode `reachctl proof verify <runId|capsule>` computes deterministic verification outputs:

- audit/Merkle root
- run fingerprint
- deterministic verification status

For capsule inputs, Reach validates the embedded run fingerprint against the capsule event log.
