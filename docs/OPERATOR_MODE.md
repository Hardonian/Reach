# Operator Mode

Run local-first operator summary:
- `reachctl operator`

Outputs federation topology counters, active runs, quarantine counts, replay alerts, denials, and error-rate summary in JSON for terminal/TUI integrations.
