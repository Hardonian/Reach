# Community Gallery (Static)

This folder is static, file-based gallery scaffolding for OSS pack discovery.

- Generated pack markdown files can be added per pack.
- Tag metadata is sourced from `pack.json` and `index.json`.
- Install snippets should use `reach pack add <source>`.
