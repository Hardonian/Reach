#!/usr/bin/env bash
set -euo pipefail

cmd="${1:-}"
case "$cmd" in
  doctor)
    shift
    (cd tools/doctor && go run . "$@")
    ;;
  release-check)
    shift
    ./tools/release-check.sh "$@"
    ;;
  *)
    echo "usage: reach doctor|release-check" >&2
    exit 1
    ;;
esac
