# Provider Adapter SDK

Implementation lives in `src/providers/sdk/index.ts` and includes:
- adapter interface
- adapter registry
- deterministic example adapter
- conformance tests in `src/providers/sdk/index.test.ts`
