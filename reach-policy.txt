# Reach Governance Policy — Default (OSS)
# This file is loaded automatically from the data directory or workspace root.
# See: reach policy show | reach policy evaluate <runId>
# Docs: docs/EVIDENCE_CHAIN_MODEL.md

version = 1

# Require packs to declare deterministic: true
require_deterministic = false

# Require a valid ed25519 signature on pack artifacts
require_signed = false

# Maximum number of external (network) dependencies allowed
# 0 = unlimited
max_external_dependencies = 0

# Require all plugins to declare a pinned checksum
require_plugin_pinned = false

# Minimum reproducibility score (0-100) for a run to be accepted
# 0 = no minimum
min_reproducibility_rate = 0

# Prevent chaos-mode execution on main/master branch
forbid_chaos_on_main = false
