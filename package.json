{
  "name": "@zeo/decision-engine-wasm",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run build:wasm && tsc",
    "build:wasm": "wasm-pack build ../decision-engine-rs --target nodejs --out-dir ../decision-engine-wasm/pkg",
    "test": "node test/parity.js",
    "benchmark": "node test/benchmark.js"
  },
  "dependencies": {
    "fs-extra": "^11.1.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "wasm-pack": "^0.12.0",
    "@types/node": "^20.0.0"
  }
}