#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run maintenance to ensure hygiene and structure
npm run maintenance

# Prevent committing if maintenance changed files (which would be unstaged)
if ! git diff --quiet; then
  echo "❌ Maintenance scripts detected and fixed hygiene issues."
  echo "   Please stage the changes and commit again."
  exit 1
fi