# Reach environment template
# Copy to .env for local development and provide real values.

# Runner auth/session settings
REACH_SESSION_COOKIE_NAME=reach_session
REACH_SESSION_TTL_HOURS=24

# Optional GitHub OAuth for services/runner (/auth/login, /auth/callback)
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_REDIRECT_URL=http://localhost:8080/auth/callback

# Optional BYOK defaults (never commit real keys)
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com

# Enterprise policy knobs
REACH_ENTERPRISE_MAX_SPAWN_DEPTH=4
